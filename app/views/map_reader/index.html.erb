<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
  <style>
      /* Email styles need to be inline */
  </style>
</head>

<body>
<h1>Card Readers</h1>

<%= link_to 'Links', '/links' %>
<%= link_to 'New Card Reader', new_map_reader_path %>
<table>
  <thead>
  <tr>
    <th>Library ID</th>
    <th>User ID</th>
    <th></th>
    <th></th>
  </tr>
  </thead>
  <tbody>
  <% @map_reader.each do |map_reader| %>
    <tr>
      <td><%= map_reader.library_id %></td>
      <td><%= map_reader.user_id %></td>
      <td><%= link_to 'Show', map_reader_path(map_reader) %></td>
      <td><%= link_to 'Edit', edit_map_reader_path(map_reader) %></td>
      <td><%= button_to 'Delete', map_reader_path(map_reader), method: :delete %></td>
    </tr>
  <% end %>
  </tbody>
</table><%= will_paginate @map_reader %>
</body>
</html>