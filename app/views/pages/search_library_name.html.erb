<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
  <style>
      /* Email styles need to be inline */
  </style>
</head>

<body>
  <h1><%= @library.pluck(:name).join(", ") %></h1>
  <p><%= @library.pluck(:address).join(", ") %></p>
  <%= link_to 'Back', user_info_path, class: 'btn btn-primary' %>
</body>
</html>